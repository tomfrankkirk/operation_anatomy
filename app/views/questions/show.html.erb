Listing questions for this level, for session params

User ID 
<%= session[:userID]%> 
QuestionsIDs from the session
<%= session[:questionIDs] %>

Printed via @user.questionIDs
<%= @user.questionIDs %>